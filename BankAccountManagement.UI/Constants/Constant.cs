namespace BankAccountManagement.UI.Constants;

internal static class CommonConstant
{
    public struct ErrorMessage
    {
        public const string DoesNotSupportRequestMethod = "The target resource doesn't support the specified request method.";
        public const string CouldNotDeserialize = "Couldn't deserialize the received data to the requested type.";
    }
}
